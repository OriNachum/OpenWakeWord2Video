# Wake Word Detection Configuration
# Copy this file to .env and customize for your setup

# Wake word model(s) to detect (comma-separated if multiple)
# Available models: hey_jarvis, alexa, hey_mycroft, hey_rhasspy
# Example: WAKE_WORD_MODELS=hey_jarvis,alexa
WAKE_WORD_MODELS=hey_jarvis

# Path to the video file to play when wake word is detected
# Can be relative or absolute path
# Example: VIDEO_PATH=/Users/username/Videos/response.mp4
VIDEO_PATH=./video.mp4

# Microphone device index (leave empty to use system default)
# Run 'python test_microphone.py' to see available devices
# Example: DEVICE_INDEX=2
DEVICE_INDEX=

# Detection threshold (0.0 to 1.0)
# Lower values = more sensitive (more false positives)
# Higher values = less sensitive (might miss detections)
# Recommended: 0.4 - 0.6
THRESHOLD=0.5

# Prediction interval in seconds (time to sleep between predictions)
# Higher values = lower CPU usage but slower detection
# Lower values = faster detection but higher CPU usage
# Recommended: 0.05 - 0.2
PREDICTION_INTERVAL=0.1


# Enable to get recordings in a rolling buffer of 5 seconds and 10 files
ENABLE_DEBUG_RECORDING=false

DEBUG_RECORDING_PATH=./debug_recordings

# Add for custom models
CUSTOM_MODEL_PATH=